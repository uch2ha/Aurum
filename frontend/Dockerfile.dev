FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

# Source is bind-mounted in docker-compose.development.yml; node_modules stays
# in a named volume so host installs never shadow the container's.
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]